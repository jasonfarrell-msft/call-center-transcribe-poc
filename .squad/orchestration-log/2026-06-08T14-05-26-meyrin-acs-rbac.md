# Meyrin Run 1: ACS Option C Bicep — RBAC, minReplicas, AudioSource__Mode

**Date:** 2026-06-08T14:05:26Z  
**Agent:** Meyrin (Backend Dev)  
**Phase:** Infrastructure delivery — Option C  
**Deliverable:** `meyrin-acs-infra-rbac.md`

## Summary

Implemented all Bicep changes per Athrun's ACS Option C sign-off.

### Changes Made

1. **ACS Data-Plane RBAC Role Assignment**
   - Extended `modules/acr-pull-role-assignment.bicep` to support `'communicationServices'` scope type
   - Added module invocation in `main.bicep` (deterministic guid naming, scoped to ACS resource)
   - Role: `Communication Services Contributor` (2b4609a5-7812-4aba-b5e3-076e6a078419)
   - Principal: ACA system-assigned managed identity
   - Justification: Minimum viable for Call Automation AnswerCall + StartMediaStreaming

2. **minReplicas = 1 (Parameter)**
   - Before: Hardcoded `minReplicas: 0`
   - After: `param apiMinReplicas int = 1`
   - Rationale: Prevents cold-start call drops during demo
   - Cost delta: ~$15–30/month for idle 0.5 vCPU (negligible for POC)
   - maxReplicas: Kept at 1 (no scale-out needed; session affinity moot on single replica)

3. **AudioSource__Mode Environment Variable**
   - Added to ACA Container App env vars: `AudioSource__Mode = 'Mock'`
   - ASP.NET Core env var config maps double-underscore to colon-separated key
   - Preserves existing default (MockAudioSource)
   - Activation: Flip to `'Acs'` via env var update (no rebuild)

### Files Changed
- `infra/main.bicep` — module invocation, minReplicas param, env var
- `infra/modules/acr-pull-role-assignment.bicep` — extended for 'communicationServices' scope
- `infra/main.parameters.json` — apiMinReplicas: 1

### Bicep Validation
- `az bicep build infra/main.bicep` → 0 errors, 0 warnings

**Status:** IMPLEMENTED; ready for coordination with Dyakka's code
