# Orchestration Log — Lacus (Speech Consumer)

**Timestamp:** 2026-06-08T19:24:21Z  
**Agent:** Lacus (AI Engineer)  
**Decision:** Speech Consumer Built — SpeechTranscriptionService  
**Decision ID:** lacus-speech-consumer-built  
**Status:** IMPLEMENTED & COMMITTED (7426ebe)

## Session Activity

1. **Implemented consumer:** `SpeechTranscriptionService : BackgroundService` in `src/CallCenterTranscription.Api/Services/SpeechTranscriptionService.cs`

2. **Key design elements:**
   - Reads IAudioSource (mode-agnostic; switches on Mode=Mock vs Mode=Acs)
   - Creates PushAudioInputStream (PCM 16-bit, 16kHz, mono)
   - Wires Recognizing→isFinal=false + Recognized→isFinal=true TranscriptEvents
   - SignalR output via "stream.transcript" group (no UI changes required)
   - Token refresh via PeriodicTimer (9min)

3. **Auth:** DefaultAzureCredential → scope cognitiveservices.azure.com/.default → aad#{resourceId}#{token} format. No keys in code/config.

4. **RBAC:** Cognitive Services User (a97b65f3) confirmed live on Speech resource by Meyrin; consumer will auth successfully.

5. **Removed DemoSafety:DataMode startup guard** per go-live sign-off decision.

6. **Coexistence:** Mock mode yields no frames → service idles; Acs mode consumes live Channel → produces transcripts; scripted feed REST endpoints unchanged.

7. **Build validation:** `dotnet build CallCenterTranscription.sln -c Release` → 0 errors.

## Decision Output

**File:** `.squad/decisions/inbox/lacus-speech-consumer-built.md` (merged to `decisions.md`)

**Commit:** 7426ebe

**Package:** Microsoft.CognitiveServices.Speech v1.50.0 (latest GA)

## Status in Go-Live Sequence

**Step 1 complete:** Consumer built + verified. Closes critical-path blocker; unblocks Steps 2–7.
