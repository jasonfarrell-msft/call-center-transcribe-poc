# Keep reusable AZD metadata and the deployment plan in git.

# Exclude per-environment local state that may contain machine-specific or sensitive values.
*/.env
*/.env.lock
*/.state.json
*/.state/
.state-change
